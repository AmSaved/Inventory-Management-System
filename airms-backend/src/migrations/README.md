# Database Migrations

This directory contains database migration files for the AIRMS application.

## How to Run Migrations

### Using Sequelize CLI

1. Install sequelize-cli globally:
```bash
npm install -g sequelize-cli